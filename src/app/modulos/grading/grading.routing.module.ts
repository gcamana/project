import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';


@NgModule({
    imports: [RouterModule.forChild([
         //{ path: '', component: QualificationModule },
    ])],
    exports: [RouterModule],

})

export class GradingRoutinModule { }