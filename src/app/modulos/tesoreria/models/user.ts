export interface IUser {
    id_user      ?: string;
    nombres      ?: string;
    first_name   ?: string;
    first_name2  ?: string;
    last_name    ?: string;
    role         ?: string;
    profile_image?: string;
    is_debtor    ?: boolean;
    on_register  ?: string;
    status       ?: string;
}
export interface IBank {
    id_bank      ?: string;
    nombres      ?: string;
    first_name   ?: string;
    first_name2  ?: string;
    last_name    ?: string;
    role         ?: string;
    profile_image?: string;
    is_debtor    ?: boolean;
    on_register  ?: string;
    status       ?: string;
}